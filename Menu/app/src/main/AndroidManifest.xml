<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.menu.menu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainHub"
            android:label="@string/title_activity_main_hub"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".Home" />
        <activity android:name=".Settings" />
        <activity android:name=".ChefSettings" />
        <activity android:name=".MealRegistration" />
        <activity android:name=".Map" />
        <activity android:name=".SearchResults" />
        <activity android:name=".MealView" />
        <activity android:name=".PaymentProcessing" />
        <activity android:name=".MealOrder" />
        <activity android:name=".MeetupChat" />
        <activity android:name=".AddressEdit" />
    </application>

</manifest>